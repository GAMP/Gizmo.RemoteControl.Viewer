<PageTitle>Viewer by params</PageTitle>

<Viewer 
    Server="@Server"
    SessionId="@SessionId"
    AccessKey="@AccessKey"
    RequesterName="@RequesterName"
    ViewOnly="@ViewOnly"
    Mode="@Mode" />
