<PageTitle>Viewer by params</PageTitle>

<Viewer 
    ServerUrl="@Server"
    SessionId="@SessionId"
    AccessKey="@AccessKey"
    RequesterName="@RequesterName"
    ViewOnly="@ViewOnly"
    Mode="@Mode" />
